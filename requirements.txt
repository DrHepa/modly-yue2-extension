# Exact direct dependency versions from YuE2 source revision in upstream.lock.json.
# torch is installed separately from the platform's selected wheel index.
transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.7.0
tiktoken==0.12.0
numpy==2.2.6
soundfile==0.13.1
accelerate==1.13.0
